source 'https://rubygems.org'

gem 'rails', '3.2.12'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'pg'      					# implementa la interface de postgres
gem 'citier'  					# implementa multiple table inheritance
gem 'rails_sql_views'   # implementa views in rails. Necesario para "citier"
gem 'will_paginate', '~> 3.0.0'   # implementa paginación en rails
gem 'dynamic_form'      # implementa metodos de ayuda para tratar en el view 
                        # errores en la validacion del modelo.
gem 'devise'            # Identificacion usuarios
gem 'strong_parameters' # necesario para utilizar params.require
gem 'cancan'            # autenticacion de que funciones un usuario puede hacer

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', :platforms => :ruby

  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'debugger'
